import NativeThemeName from '@shared/theme/nativeThemeName'
import ThemeName from "@shared/theme/themeName"

export default interface ThemeSettingsI {
    nativeThemeName: NativeThemeName,
    themeName: ThemeName
}